export class Person{
  id:number;
  name:string;
  address:string;
  blacklistet:boolean; // simulere if sætning senere
  date:Date; // dette er vist et interface
  // skal lige have date med -interface
}
